/target
/ui/node_modules
/ui/dist
.env
*.pending-snap

# Regenerated by tauri-build on every compile.
/crates/coral-app/gen/schemas

# RustRover's MCP endpoint port differs per install, so this stays machine-local.
/.mcp.json

# IDE: share run configurations, keep everything else local.
/.idea/*
!/.idea/runConfigurations/

# Design references the owner supplied. Third-party screenshots: never committed.
/.reference
